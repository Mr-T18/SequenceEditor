# ロボットアクチュエータ・シーケンス制御データ管理システム 詳細仕様書

## 1. システム概要
本システムは、ロボットに使用する個々のモータの基本情報を登録・管理し、それらをタイムライン上に配置して制御パラメータ（方向、出力、時間、目標量）を設定した「シーケンス」を構築するためのデスクトップアプリケーションである。
時間軸上の1つのステップにおいて、最大3つのモータを同時に動作させる「同時駆動（並列動作）」に対応している。

---

## 2. 技術スタック・動作環境
* **言語:** Python (Version 3.14.4)
* **GUIフレームワーク:** customtkinter, tkinter, ttk
* **データ管理形式:** JSON (データ永続化用・分離ファイル形式), CSV (ロボット用最終出力)
* **ファイル配置ルール:**
  * プログラムが使用する設定ファイルは `json/motor.json` および `json/sequence.json` として `json/` ディレクトリ内に保存。
  * ロボット用の成果物 `sequence.csv` はルートディレクトリ（アプリと同じ場所）に直接出力。
* **OS固有設定:** Windowsの高DPI（高解像度）環境での描画のブレやボタンの点滅を抑制するため、起動時にDPI認識モード（`SetProcessDpiAwareness`）を有効化している。

---

## 3. データ構造・クラス設計

モータの基本情報（共通定義）と、シーケンス内での具体的な動作（ステップ）を完全に切り離し、同時駆動を表現できる2次元構造として設計する。

### 3.1 Motor クラス（モータ基本情報）
個々のモータを識別するためのデータモデル。属性はプライベート変数として隠蔽し、プロパティを介して読み出しを行う。
* **メンバ変数（属性）:**
  * `__name` (str): モータ名（識別キー）
  * `__canID` (int): CAN通信用ノードID（1〜254）
* **主要メソッド:**
  * `to_dict()`: `{"name": "...", "canID": ...}` の辞書型を返す。

### 3.2 SequenceStep クラス（動作ステップ）
シーケンスのタイムライン上に配置される「1つの動作指示」を表すデータモデル。登録された `Motor` オブジェクトへの参照と、そのステップ固有の制御パラメータを保持する。
* **メンバ変数（属性）:**
  * `motor` (Motor): 登録されたモータオブジェクトへの参照
  * `dir` (int): 回転・移動方向（1: CW, 2: CCW、デフォルト初期値: 1）
  * `duty` (int): 出力比率（0〜100%、5刻み、デフォルト初期値: 0）
  * `timeout` (int): タイムアウト時間（ms、100刻み、デフォルト初期値: 0）
  * `target` (int): 目標制御量（0〜100、使用しない場合は `-1`、デフォルト初期値: -1）

### 3.3 Sequence クラス（動作手順の組）
`SequenceStep` インスタンスを時系列（ステップ単位）で管理するクラス。同時駆動に対応するため、内部ステップは2次元リスト構造をとる。
* **メンバ変数（属性）:**
  * `__name` (str): シーケンス名
  * `__steps` (List[List[SequenceStep]]): 同時駆動するステップのリスト（最大3要素）を時間軸順に格納するネストリスト（**最大10ステップ制限**、自動上詰め）
* **主要メソッド:**
  * `add_step(step)`: リストの末尾に新しいステップを追加。現在のステップ数が10以上の場合は `False` を返す。
  * `remove_step(index)`: 指定位置のタイムラインステップ（同時駆動の塊ごと）を削除し、自動で上詰めする。
  * `to_dict()`: 2次元リスト構造に対応した状態で辞書型（キー名 `"motor_name"`）に変換して返す。

### 3.4 Manager クラス（データ総括マネージャー）
アプリ全体のプール情報を一元管理し、文字コードの自動判別読み込みやデータの防衛・出力を行うクラス（シングルトンパターン）。
* **メンバ変数（属性）:**
  * `motors` (dict): 登録済みの全モータを保持（キー: モータ名）
  * `sequences` (dict): 作成済みの全シーケンスを保持（キー: シーケンス名）
  * `export_slots` (dict): 最終搭載スロット（1〜9番）へのシーケンス割り当て状態
  * `has_changes` (bool): 未保存の変更が存在するかを示すフラグ
* **データ読み込み互換ロジック (`load_all`)**:
  * ファイル読み込み時、Windows環境に混在しやすい文字コード（`utf-8-sig`, `utf-8`, `cp932`）を順次自動試行してデコードエラーを防止する。
  * 旧仕様の「1ステップ＝1モータ」の1次元JSONデータが検出された場合、自動的に同時駆動用の2次元リスト構造にラップして復元する互換パース機構を持つ。

---

## 4. 画面仕様およびGUI要件（ui/ パッケージ）

操作ミスの防止と視認性を重視し、ウィジェットおよびリストボックスの文字サイズは一律 **`18pt`** に拡大して表示する（下部パラメータ編集エリアのタイトル等は `16pt, bold`）。
また、複数のリストボックス間での選択の干渉・消失を防ぐため、すべての `Listbox` に **`exportselection=False`** を設定し、ピンポイントで選択行のパラメータをロードできるよう単一選択仕様で運用する。

### 4.1 タブ1：Motor共通設定画面
* モータの名前とCAN ID（1〜254）の組み合わせを登録・編集・管理する画面。
* 既存のモータ名を編集（改名）した場合、**そのモータを使用しているすべてのシーケンス内の該当ステップも、自動的に新しいモータ情報へと参照が追従・連動更新**される。
* シーケンス側で1回でも使われているモータは、安全ロックのため削除できない。
* テキストカーソルの点滅（ブリンク）に伴うボタンのチラツキを完全に遮断するため、編集領域コンテナの背景色には明示的な固定グレー（`#f0f0f0`）を指定している。

### 4.2 タブ2：Sequence作成・編集画面
* **左ペイン:** 登録されているモータの名前一覧リスト。
* **中央ボタン（追加UIの刷新）:** 操作意図を明確に区別するため、以下の2つの専用ボタンを配置する。
  * `新規ステップとして追加 ➔`: 選択したモータを、独立した新しい時間軸ステップとして末尾に追加する。
  * `選択ステップに同時駆動を追加 ➔`: 現在タイムラインで選択しているステップの内部に、同時駆動モータとして吸着追加する（1ステップ内最大3個まで。同一ステップ内でのモータ重複はエラーを返す）。
* **右ペイン:** 現在のシーケンスのタイムライン（リストボックス）。
  * **ヘッダー行:** 「`ステップ 01: モータ名A / モータ名B`」のように、そのステップに含まれるすべてのモータ名がスラッシュ区切りで一目でわかる形式で表示される。
  * **詳細行:** 先頭の余計なスペースを排除して左端に密着させ、記号（`┣` や `┗`）を用いて「`┗ 1. モータ名A (ID:xx, CW, 100%, 1000ms, 目標:未使用)`」の形式で階層表示する。
  * **並び替えと削除:** ▲上へ、▼下へによるステップ（塊ごと）の順序入れ替えに対応。右側の削除ボタンは、ヘッダー行選択時は「`ステップ全体削除`」、詳細行選択時は「`同時モータ削除`」へとテキストと処理内容が動的に切り替わる。
* **【画面下部インライン編集領域】:**
  * 高解像度スケーリング環境においてプルダウン矢印の内部に白い斑点（ポリゴン描画抜けバグ）が発生するのを防ぐため、コンボボックスの代わりに **`CTkOptionMenu`**（`height=38`）を採用して美しい下矢印を描画する。
  * 左上のタイトルテキストを「`ステップ編集`」に短縮し、その右側に現在編集している対象のステップ構成（例：「` : ステップ 01: モータ名`」）を動的に表示する。
  * 画面最大化時にコントロールが右側に押し潰されてガタつくのを防ぐため、列の引き伸ばし重みを最右端に逃がした**左詰め4列レイアウト（column 0〜3）**に最適化し、縦の整列ラインを綺麗に統一している。
    * `column 0`: 項目ラベル（width=140, sticky="w"）
    * `column 1`: 動作方向OptionMenu（width=320） / タイムアウト（スライダーwidth=200 ＋ Spinbox ＋ 単位）
    * `column 2`: 項目ラベル（左側に35pxの間隔を設定、sticky="w"）
    * `column 3`: 出力比率 / 目標制御量（スライダーwidth=220 ＋ Spinbox ＋ 単位 ＋ 末尾に「使用しない」チェックボックスを内包配置）

### 4.3 タブ3：最終搭載スロット設定画面
* Slot 1 から Slot 9 までのプルダウンメニュー（`CTkComboBox`）によるシーケンス割り当て画面。
* すべての文字サイズを `18pt` に拡大し、割り当て内容とCSV出力を連動管理する。

---

## 5. ロボット専用CSV出力フォーマット
データ構造は同時駆動（2次元）に拡張されるが、マイコン側のパース仕様を満たすため、生成される `sequence.csv` は以下のフォーマットに厳密に従う。
* **総行数:** 1スロットにつき10行 × 9スロット ＝ **計90行**の縦並び固定（ヘッダー等は一切出力しない）。
* **1モータ動作時の行**: `canID,duty,timeout,dir,target` の5パラメータのみが出力され、右側に余分なカンマや空白を付加せずに即改行する（可変長形式）。
* **同時駆動（2〜3モータ）動作時の行**: 個々のモータの5パラメータの間に、**半角スペース1つを挟む空白列**を挿入してカンマで結合する。3つに満たない場合はその直後で即改行する。
  * *出力例（2並列）:* `2,30,4400,1,-1, ,1,85,1000,1,-1`
* **空きデータの埋め尽くし**: シーケンスが未割り当て（`None`）のスロット、または10ステップに満たない時間軸上の空き枠については、5つのパラメータすべてを `n` で埋めた **`n,n,n,n,n`** を出力し、ファイル全体の90行構造を完全に維持する。
* **target未使用の挙動**: targetが未使用（`-1`）のモータステップについては、従来通り `target` 部分に `-1` をそのまま書き込んで出力する。

---

## 6. ウィンドウ終了時の安全対策
* 変更が保存されていない（`has_changes == True`）状態で「×」ボタンが押された場合、安全のため画面中央にカスタム3択ダイアログ（`UnsavedChangesDialog`）をポップアップ表示する。
  * 「保存して終了」: `json/motor.json` と `json/sequence.json` を最新の2次元並列構造で一括更新してから安全にアプリケーションを終了する。
  * 「保存せずに終了」: 変更内容を全て破棄して終了する。
  * 「キャンセル」: 終了処理を取り消し、現在の編集画面へと復帰する。